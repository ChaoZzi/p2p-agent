# 进度与交接（新会话先读这个）

> 更新时间：**2026-09-22（P0-01 已交付并通过 Hermes 审核）**　项目根：`D:\Projects\interview-project\p2p-agent`

## 当前状态
- **阶段**：**P0-01 已完成**（dsh 交付 → Hermes 独立审核通过）。仓库内已有可运行的双语言最小链路；下一步是 **用户亲手验收 → 进 P0-02**。
- **代码**：`java-service/`（Spring Boot 3.5.16 / JDK 21 / SQLite 幂等表）、`python-service/`（uv + httpx 客户端）；仓库 35 个跟踪文件、**11 个 commit**，已推送到远端。
- **工作区**：干净。`target/`、`.venv/`、`*.db`、`.idea/`、`out/` 均已被 `.gitignore` 覆盖（实测）。
- **运行中**：dsh 留下的 mock-oa 实例可能还在 `:8000`（`java.exe`）；Hermes 审核用的 `:8099`/`:8098` 实例已关闭。

## 远端仓库（GitHub）
- **地址**：https://github.com/ChaoZzi/p2p-agent ｜ 账号 `ChaoZzi` ｜ **public** ｜ 默认分支 `main`
- 远端 HEAD = 本地 HEAD（11 commits，含 P0-01 的 3 个 commit）；描述 + 12 个 topic 已设；提交全部关联到 `ChaoZzi`（`2748788190@qq.com` 是账号已验证邮箱）。
- **推送通道**：HTTPS + token（`.env` 的 `GITHUB_TOKEN`；**SSH 未注册**）。
- ⚠ **首次手动 push 会弹 GCM 登录**（全局 `credential.helper=manager`）；先自己 `git push` 一次把凭据缓存进 Windows 凭据管理器，之后 dsh/IDEA 里的 push 才不会卡弹窗。
- **git 走代理（2026-09-22 实测后定）**：github **直连不通（curl 000 / 15s 超时）、走 `127.0.0.1:7897` 代理 200（0.5s）**；已配 `http.https://github.com.proxy=http://127.0.0.1:7897`。⚠ **关掉代理时要把这条清掉**。教训：本机"直连 vs 代理"会随时段/模式翻转，**动手前先双侧 curl 测一遍**。
- 推送时偶尔出现 `cannot lock ref ... is at <新值> but expected <旧值>` —— 代理下请求重发的假警报，**以 `git ls-remote`/REST API 的最终 SHA 为准**。
- **公开前扫描**：跟踪文件与历史**无**密钥/隐私（§ 见下"审核记录"）。

## P0-01 审核记录（Hermes，2026-09-22）
**方法（不采信 dsh 的自测结论，全部自己重跑）**：
1. `git clone`  **本地仓库**到临时目录（只含已提交内容）→ `mvn package` **编译通过**（jar 38.8MB）→ 证明"提交内容自洽、无本地私有依赖"；
2. 换端口 `:8099` + **全新 SQLite 库** 起实例 → 自己写 21 项断言跑通（trace 回显/幂等双调/4 类 422/非法 trace 兜底/故障延迟实测 3026ms/负值 422/超限截断/5 并发 1×false+4×true）；
3. **自己查库**（sqlite3）：同 request_id 只有 1 行、`trace_id` 落库 = Python 生成的那个 uuid、`response_json` 里 `dedup=false`、`approval_id` 与接口返回一致；
4. **自己 grep 日志**：另起 `:8098`（日志落盘）→ 日志里 `trace_source=incoming`、业务行带同一 uuid、幂等命中行 `first_trace_id == current_trace_id`；
5. **用 dsh 的 `oa_client.py` 打我的实例**（含中文申请人"姜盛超"）→ SELFTEST PASS（证明 UTF-8 跨语言无损）；
6. **契约新鲜度**：现场重新导出 `/v3/api-docs` 与提交的 `docs/contracts/java-service-openapi.json` **逐字节一致**。

**结论：7 条验收全部通过 ✓**（独立证据，非引用 dsh 的输出）。

**dsh 主动上报的 7 处"超出简报"的决定 → 审核复核**：
| # | 决定 | 复核 |
|---|---|---|
| 1 | `X-Request-Id` 必须与 `body.request_id` 一致，否则 422 | **接受**（幂等键两个来源，不允许各说各话） |
| 2 | 首次响应也显式回 `X-Dedup: false` | **接受**（对称；Python 断言已依赖它）→ 需同步进契约文档 |
| 3 | `timeout_ms<0` → 422；`>10000` → 截断 10000 + WARN | 接受（与契约上限一致） |
| 4 | `random_delay` 抖动 0–500ms | 接受 |
| 5 | `PUT /api/mock/faults` 字段 null = 保持原值 | 接受（局部更新语义，与包装类型配套） |
| 6 | 故障延迟只打 mock 写端点，不打 `GET /api/mock/faults` | 接受（否则关开关也要等 5 秒） |
| 7 | 新增 `python-service/uv.lock` 入库 | 接受（跨机器可复现） |

**非阻塞改进项（留给 P0-02 顺手做，不返工）**：
- `server.port=8000` 硬编码在 `application.properties` → 建议改 `${SERVER_PORT:8000}`（否则 P0-02 起 Python 时端口不可配）；
- `GET /healthz` 也被 TraceFilter 打访问日志（探活噪音）→ 可在过滤链里排除 `/healthz`；
- **MDC 是 ThreadLocal**：P0-02 引入异步/虚拟线程/线程池时，trace 必须显式传递（dsh 已在讲解 §6.4 标注这条边界）；
- SQLite 连接池=1 是"用串行换确定性"，P0-02 换 PG 时再放开。

**流程备注（如实记录）**：dsh 已按协作规范自行 commit（`feat(java)` / `feat(python)` / `docs:讲解.md`），这 3 个 commit 在 Hermes 审核**之前**就随仓库一起推到了 GitHub（因为当时正在建远端仓库）。审核结论为通过，故无需回退；**后续卡建议改为"审核通过后再 push"**。

## 环境（2026-09-22）
- **JDK**：21.0.9 LTS ✓，`JAVA_HOME=C:\Program Files\Java\jdk-21`（已持久化）。
- **Maven**：3.9.9 ✓ `D:\software\maven-dist\apache-maven-3.9.9`（`bin` 在用户 PATH；cmd 下 `mvn -v` 正常）。
  - ⚠ 坑 1：HKCU 的 `MAVEN_HOME` 指向**已不存在**的 `D:\JavaDT\...`，别照它找 Maven。
  - ⚠ 坑 2：Hermes 的 git-bash 里跑 `mvn` 会报 `classworlds ClassNotFoundException`（MSYS sh 包装器问题，非 Maven 坏）→ 用 `mvn.cmd` 全路径。
  - ⚠ 坑 3（dsh 实录）：pom 注释里**不能出现连续两个减号**，否则 Maven 报 `Non-parseable POM`。
- **依赖源**：`~/.m2` 约 2.0GB+；镜像已修为 **`https://maven.aliyun.com/repository/public`**（原 HTTP 老入口被 Maven 3.8.1+ 的 http-blocker 封杀；`<id>` 保持 `alimaven` 以免缓存失效）；备份在 `~/.m2/settings.xml.bak-20260919`。
- **代理（`127.0.0.1:7897` verge-mihomo）**：表现**随模式/时段翻转**（9-19 掐断 maven 系 TLS；9-22 全部 200）。Maven/Java **不读** `HTTP_PROXY`（Maven 只认 settings.xml 的 `<proxies>`，未配）→ Maven 永远直连，无需代理配置。curl 打本机要 `--noproxy '*'`。
- **Python**：3.11.16 + uv 0.12.7 ✓；`python-service/.venv` 由 uv 管理。中文输出需 `PYTHONUTF8=1 PYTHONIOENCODING=utf-8`。
- **dsh CLI**：v0.1.1-rc.2（`dsh web` 交互界面 / `dsh --profile headless "任务"` 一次性）；`tui` profile 未装。
- **网络**：`curl --ssl-no-revoke -4`；`start.spring.io` 不可达（pom 需手写）。

## IDE 配置（IDEA Ultimate 2023.2.8）
- 项目 SDK 已是 **21** + language level **21**（`.idea/misc.xml` 已确认）；剩 `.idea/p2p-agent.iml` 里 `LANGUAGE_LEVEL="JDK_17"`（root 模块无源码，无害）。
- Maven home 已指向 3.9.9 ✓；`.idea/` 已被 git 忽略 ✓。
- 语法只用 pom 的 `java.version=21` 为准（`--release 17` 会连 `Thread.ofVirtual()` 等 21 API 一起锁死，已实测）。
- Python 侧用 **PyCharm 2026.1.2**（`D:\DevTools\PyCharm 2026.1.2`）单独打开 `python-service`。
- 注意：IDEA 运行时别手改 `.idea/*.xml`（会被覆盖）。

## 角色与规则（详见 docs/协作规范.md）
- **Hermes**：需求/架构/审核（读代码 + 读讲解 + 本机跑自测）。
- **dsh**：编码执行，只写 `[dsh写]`/`[dsh写+讲]` 档文件，交付必须附《讲解.md》+ 自测证据。
- **用户**：亲手敲 `[你敲]` 档（核心理解件）、验收、commit 把关。
- 纪律：一张卡 = 一个可演示增量 + 一个 commit；dsh 只 commit 自己的文件；**审核通过后合入/push**。

## 下一步
1. **用户亲手验收 P0-01**（两条命令，亲手看到 trace 与幂等）：
   ```bash
   D:\software\maven-dist\apache-maven-3.9.9\bin\mvn.cmd -f java-service/pom.xml spring-boot:run
   cd python-service && uv run python -m tools.oa_client
   ```
   期望：Python 打印的 uuid 在 Java 控制台里能 grep 到；第二次调用 `dedup=true`。
2. **P0-02 最小闭环**（对话→意图→建单→审批→完成→审计）。`[你敲]` 档 5 个：Java `FlowStateMachine` / `FlowService` / `AuditService` ＋ Python `harness/tool_registry.py` / `harness/agent.py` / `agents/intent.py`。
3. 之后 P0-03 补 P2P 主链路（预算/下单/付款）→ P1 完备（幂等/补偿/评测/指标）→ P2 加分。

## 文档地图
| 文件 | 作用 |
|---|---|
| `docs/PRD.md` | 需求 v0.2：方案取舍、边界、§8 FAQ |
| `docs/architecture.md` | 契约、状态机、双语言分工 |
| `docs/协作规范.md` | 三档代码归属、讲解标准、commit 纪律 |
| `docs/task-cards/P0-01-smoke.md` | 已完成的卡（双服务冒烟，验收标准） |
| `docs/task-cards/P0-01-dsh-brief.md` | 该卡的执行简报（字段级契约） |
| `docs/task-cards/P0-01-讲解.md` | dsh 交付的逐文件讲解 + 自测原始输出（33KB） |
| `docs/contracts/java-service-openapi.json` | Java 契约快照（已与运行实例 diff 一致） |
| `docs/task-cards/P0-02-min-loop.md` | **下一张卡**：最小闭环 |
| `docs/评审报告-面试项目评估.md` | dsh 交叉评审原件 |
| `docs/进度.md` | 本文（新会话入口） |

## 新会话开场模板（复制即用）
> 读 `D:\Projects\interview-project\p2p-agent\docs\进度.md` 和 `docs\协作规范.md`，继续执行；代码分档与 commit 规则不变（审核通过后再 push）。

## 常见追问的固定答案
- 为什么双语言：确定性流程给 Java 工程，模型决策给 Python 生态（对齐 JD 的运行时拆法）。
- 为什么不用 RAG / 不用现成工作流引擎：见 PRD v0.2 §8 FAQ。

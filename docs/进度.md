# 进度与交接（新会话先读这个）

> 更新时间：**2026-09-19**（本次为事实更正）　项目根：`D:\Projects\interview-project\p2p-agent`

## 当前状态
- **阶段**：P0-01（双服务冒烟）**待启动**。任务卡已就绪（`task-cards/P0-01-smoke.md`），但**尚未交给 dsh 执行**，仓库内**零代码**。
- **仓库**：分支 `master`（未配远端），3 个 commit 全为 docs（`3350c67` → `bc86b6b` → `5115da8`）；工作区干净。
- **缺交付物**：`java-service/`、`python-service/`、任何《讲解.md》**都不存在**（已全盘核实）。
- **运行中**：无。:8000 / :8001 无监听，无 java/python 进程。

## 环境（2026-09-19 实测，取代此前口头结论）
- **JDK**：21.0.9 LTS ✓，`JAVA_HOME=C:\Program Files\Java\jdk-21`（已持久化）。
- **Maven**：3.9.9 ✓ 位于 `D:\software\maven-dist\apache-maven-3.9.9`，其 `bin` **确实已在用户 PATH**；在 **cmd / PowerShell 里 `mvn -v` 正常**（Maven home 识别正确）。
- ⚠ **坑 1**：HKCU 环境变量 `MAVEN_HOME` 指向**已不存在**的 `D:\JavaDT\IDEA\software\apache-maven-3.9.9-bin\apache-maven-3.9.9`（陈旧值）。`mvn.cmd` 不依赖它所以没炸，但**不要照它找 Maven**；有空可清理。
- ⚠ **坑 2**：在 Hermes 的 git-bash 里直接跑 `mvn` 会报 `ClassNotFoundException: org.codehaus.plexus.classworlds.launcher.Launcher`——这是 **MSYS 下 `bin/mvn` sh 包装器的路径问题，不是 Maven 损坏**。要用就走 `D:\software\maven-dist\apache-maven-3.9.9\bin\mvn.cmd`（或原生终端）。
- **依赖源**：`~/.m2/repository` 约 2.0 GB+（已预热 sqlite-jdbc 3.53.4.0、Spring Boot 3.5.16 parent）。⚠ **2026-09-19 修过一个硬阻塞**：`~/.m2/settings.xml` 的镜像原本是 HTTP 老入口 `http://maven.aliyun.com/nexus/content/groups/public/`，**被 Maven 3.8.1+ 的 http-blocker 直接封杀**（报 `Blocked mirror for repositories`），任何不在缓存里的依赖都拉不下来；已改为 **`https://maven.aliyun.com/repository/public`**（`<id>` 保持 `alimaven`，否则 2GB 缓存按 id 校验会全部失效重下）。原文件备份在 `~/.m2/settings.xml.bak-20260919`。已实测 `mvn dependency:get` 成功（sqlite-jdbc 12MB @5.4MB/s）。
- **代理关系（2026-09-19 实测，别再试）**：本机代理 `127.0.0.1:7897`（verge-mihomo）**对 Maven 系域名会掐断 TLS**——curl(schannel) 与 Java(JSSE) 双栈实测均失败（`SSLHandshakeException: Remote host terminated the handshake`），而 baidu 走同一代理正常。结论：**不要给 Maven 配代理**。好在 Java 不读 `HTTP_PROXY` 环境变量、Maven 只认 settings.xml 的 `<proxies>`（我们没有配），所以**代理开着 Maven 也是直连**，而直连 `maven.aliyun.com`(150ms) 与 `repo1.maven.org`(0.9s) **均 200**。IDEA 里如遇代理报错，把 Settings → HTTP Proxy 设为 **No proxy**。
- **版本基线（本卡建议）**：Spring Boot **3.5.16**（镜像可取；离线兜底：缓存里已有 **3.4.2** 全套 starter），Java 21，sqlite-jdbc 3.53.4.0，springdoc `springdoc-openapi-starter-webmvc-ui` 2.8.x（Boot 3.5 兼容线）。
- **Python**：3.11.16 + uv 0.12.7 ✓（与卡中记录一致）。
- **网络铁律**：外网请求**不要走系统代理**（代理下 TLS 握手失败）；直连国内镜像，curl 加 `--ssl-no-revoke -4`。不要用 start.spring.io（本机不可达）。
- **dsh CLI**：v0.1.1-rc.2 已装（`C:\Users\姜盛超\AppData\Local\hermes\node\dsh`）。

## IDE 配置体检（IDEA Ultimate 2023.2.8，2026-09-19）
- 项目已用 IDEA 打开仓库根目录；`.idea/` 已被 git 忽略 ✓（`.gitignore:31`）；`D:\` 在 IDEA 全局信任列表内，非 Safe Mode ✓。
- **Maven home** 已指向 `D:\software\maven-dist\apache-maven-3.9.9` ✓（记录在 `.idea/workspace.xml` 的 `MavenImportPreferences`）。
- ⚠ **唯一必须改的一项**：项目当前绑在 **corretto-17**（IDEA 日志证实正按 `C:\Users\姜盛超\.jdks\corretto-17.0.11` 索引；`.idea/misc.xml` 里 `project-jdk-name="corretto-17"` 与 `languageLevel="JDK_21"` 自相矛盾）。→ **Project Structure → Project：SDK = `21`（`C:\Program Files\Java\jdk-21`）、Language level = `21`**，与 pom 的 `<java.version>21</java.version>` 三处一致。
  - **为什么定 21 而不是 17（2026-09-19 实测）**：`--release 17` 会把 **API 也锁到 17** —— `javac --release 17` 编 `Thread.ofVirtual()` 直接报"找不到符号"，`--release 21` 才通过（运行输出 `VirtualThread[...]`）。本项目 IO 密集（调 LLM / 调 Java 服务 / 等审批），**虚拟线程是真实技术卖点**，不能用 17 自断。
  - 注意三者关系：**SDK = 用哪套 JDK；Language level + pom `java.version` = 允许用哪些语法与 API**。JDK 21 能编 17 的代码，反之不行。真源是 **pom**，IDE 级别只影响标红提示；若 2023.2.8 对某处 21 语法误报，只调 IDE 级别，**不要动 pom**。
- Python 侧用 **PyCharm 2026.1.2**（`D:\DevTools\PyCharm 2026.1.2`）单独打开 `python-service`；**不要**把 python-service 挂进 IDEA 项目（会互相索引 `.venv` / `target`，两边都卡）。
- IDEA 的 `Settings → HTTP Proxy` 建议设 **No proxy**（本机代理会掐断 maven 系域名 TLS）。
- 注意：IDEA 正在运行时，别手改 `.idea/*.xml`（会被覆盖）——改配置一律走 GUI。

## 角色与规则（详见 docs/协作规范.md）
- **Hermes**：需求/架构/审核（读代码 + 读讲解 + 本机跑自测）。
- **dsh**：编码执行，只写 `[dsh写]` / `[dsh写+讲]` 档文件，交付必须附《讲解.md》+ 自测证据。
- **用户**：亲手敲 `[你敲]` 档（核心理解件），验收，commit 把关。
- 纪律：一张卡 = 一个可演示增量 + 一个 commit；dsh 只 commit 自己的文件。

## 下一步
0. **（进行中）用户重读** `PRD.md` / `architecture.md` / `task-cards/P0-01-smoke.md`，确认后开工。
1. **把 P0-01 卡交给 dsh**（此前遗漏的一步）→ dsh 交付 Java 骨架 + mock-oa + Python client，附《讲解.md》+ 自测证据 → Hermes 审核（对照讲解与代码、跑自测）→ 用户验收合入。
2. P0-02 最小闭环：对话→意图→建单→审批→完成→审计。`[你敲]` = Java 状态机/FlowService/审计 + Python agent loop/工具注册/意图抽取。
3. 之后 P0-03 补全 P2P 主链路（预算/下单/付款）→ P1 完备（幂等/补偿/评测/指标）→ P2 加分。

## 文档地图
| 文件 | 作用 |
|---|---|
| `docs/PRD.md` | 需求 v0.2：方案取舍、边界、§8 FAQ |
| `docs/architecture.md` | 契约、状态机、双语言分工 |
| `docs/协作规范.md` | 三档代码归属、讲解标准、commit 纪律 |
| `docs/task-cards/P0-01-smoke.md` | 当前卡：双服务冒烟（本卡契约以它为准） |
| `docs/task-cards/P0-02-min-loop.md` | 下一张卡：最小闭环 |
| `docs/评审报告-面试项目评估.md` | dsh 交叉评审原件 |

## 新会话开场模板（复制即用）
> 读 `D:\Projects\interview-project\p2p-agent\docs\进度.md` 和 `docs\协作规范.md`，继续执行；代码分档与 commit 规则不变。

## 常见追问的固定答案
- 为什么双语言：确定性流程给 Java 工程，模型决策给 Python 生态（对齐 JD 的运行时拆法）。
- 为什么不用 RAG / 不用现成工作流引擎：见 PRD v0.2 §8 FAQ。

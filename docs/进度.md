# 进度与交接（新会话先读这个）

> 更新时间：2026-09-04　项目根：`D:\Projects\interview-project\p2p-agent`

## 当前状态
- **阶段**：P0-01（双服务冒烟）已交给 dsh 执行，**等待其交付后由 Hermes 审核**。
- **环境已就绪**：JDK 21（JAVA_HOME 已持久化）、Maven 3.9.9（`D:\software\maven-dist\apache-maven-3.9.9`，已入 PATH）、`~/.m2` 已配 aliyun 镜像（缓存约 2GB）；网络规则=外网直连国内镜像 + `curl --ssl-no-revoke -4`，不要走系统代理。
- **仓库**：git 已初始化（提交：docs 入库 → P0-01 前置更新）。
- **文档**：PRD v0.2、architecture.md、协作规范.md、task-cards/P0-01-smoke.md、task-cards/P0-02-min-loop.md、评审报告-面试项目评估.md（dsh 交叉评审原件）。

## 角色与规则（详见 docs/协作规范.md）
- **Hermes**：需求/架构/审核（读代码 + 读讲解 + 本机跑自测）。
- **dsh**：编码执行，只写 `[dsh写]` / `[dsh写+讲]` 档文件，交付必须附《讲解.md》+ 自测证据。
- **用户**：亲手敲 `[你敲]` 档（核心理解件），验收，commit 把关。
- 纪律：一张卡 = 一个可演示增量 + 一个 commit；dsh 只 commit 自己的文件。

## 下一步
1. dsh 交付 P0-01 → Hermes 审核（对照讲解与代码、跑自测）→ 用户验收合入。
2. P0-02 最小闭环：对话→意图→建单→审批→完成→审计。`[你敲]` = Java 状态机/FlowService/审计 + Python agent loop/工具注册/意图抽取。
3. 之后 P0-03 补全 P2P 主链路（预算/下单/付款）→ P1 完备（幂等/补偿/评测/指标）→ P2 加分。

## 新会话开场模板（复制即用）
> 读 `D:\Projects\interview-project\p2p-agent\docs\进度.md` 和 `docs/协作规范.md`，继续执行；代码分档与 commit 规则不变。

## 常见追问的固定答案
- 为什么双语言：确定性流程给 Java 工程，模型决策给 Python 生态（对齐 JD 的运行时拆法）。
- 为什么不用 RAG / 不用现成工作流引擎：见 PRD v0.2 §8 FAQ。

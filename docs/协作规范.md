# 协作规范 — P2P Agent 开发流程（v1.0）

> 角色：**Hermes（我）**= 需求/架构/审核；**dsh（DeepSeek harness）**= 编码执行；**你** = 核心代码手写 + 验收 + commit 把关。

## 1. 分工原则
- 需求与任务卡：Hermes 写，你确认后生效。
- 代码分三档，每张卡逐一标注：
  - `[你敲]`：核心理解件，你亲自写（面试追问深水区：agent loop / 状态机 / HITL / 幂等 等）。Hermes 提供行为规格，规格细到你能直接照着敲。
  - `[dsh写+讲]`：脚手架 / 配置文件 / 依赖 / 样板 / 基础设施。dsh 交付时必须附**《代码讲解》**：每个文件 1–3 句作用 + 关键设计决策。
  - `[dsh写]`：纯支持件（DTO / 测试数据 / 工具脚本），review 即可。
- 审核闭环：dsh 交付 → **Hermes 先审**（正确性 + 讲解与代码一致）→ 你验收 → 合入 → 下一张卡。

## 2. 讲解要求（dsh 交付物标准）
每张卡交付包含：
1. 代码（只含本卡负责文件）
2. `讲解.md`：逐文件说明"这段代码的作用是什么、为什么这么写、改了会怎样"
3. 自测结果：能跑的证据（命令输出/截图）

Hermes 审核时检查：讲解是否与代码一致、是否覆盖关键决策、有无含糊带过。

## 3. Commit 纪律
- 一个功能 = 一个 commit；message 用 `feat:` / `fix:` / `docs:` 前缀。
- **你敲的文件由你 commit；dsh 只 commit 自己负责的文件**（dsh 提交时排除 `[你敲]` 文件）。
- git 历史 = 面试证据：你的 commit 是你亲手写的部分。

## 4. 阶段推进（最小系统优先）
1. **P0-01**：双服务骨架冒烟（连通性，dsh 为主）
2. **P0-02**：最小闭环——对话→意图→建单→审批→完成→审计（每个架构层都碰一遍，你借它建立全局认知）
3. P0-03…：补全 P2P 主链路（预算/下单/付款）→ P1 完备 → P2 加分

## 5. 需求变更
- 口头提出 → Hermes 落到对应任务卡/PRD（版本 +1）→ 只影响未开始的卡；已开始的卡记录变更说明。

## 6. 环境事实（避免重复踩坑）
- JDK 21 LTS 已装；Maven 未装（`scoop install maven`）；Python 3.11 + uv 0.12.7。
- DeepSeek key：复用 `~/AppData/Local/hermes/.env` 的 DEEPSEEK_API_KEY → 写入 `python-service/.env`（已 gitignore，不入库）。
- 项目根：`D:\Projects\interview-project\p2p-agent`；工作区锚 `D:\Projects\interview-project`。
